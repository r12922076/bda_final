# Data Acquisition Process

The project uses public digital-trace evidence and static data snapshots.

Sources include official documentation, public script listings, competitor pages, pricing pages, and manually coded public evidence rows. Private, paywalled, login-only, or restricted content is not required by the deployed prototype.

Before deployment, data is converted into CSV and JSON files under `docs/data/`. The website does not perform runtime crawling or external downloads.
